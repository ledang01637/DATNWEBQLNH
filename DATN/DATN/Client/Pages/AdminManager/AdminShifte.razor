@page "/adminshifte"
@using DATN.Shared
@layout LayoutAdmin
@inject HttpClient httpClient
@inject NavigationManager Navigation

<style>
    .product-table-container {
        max-height: 600px;
        overflow-y: auto;
    }

        .product-table-container table {
            width: 100%;
        }

    .search-bar, .filter-bar {
        margin-bottom: 20px;
    }
</style>

@if (isLoaded)
{
    <div class="col-md-12 grid-margin stretch-card p-5">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Account</h4>
                <p class="card-description">
                    <button class="btn btn-success" @onclick="() => CreateShifte()">Add new shifte</button>
                </p>
                <div class="search-bar">
                    <input type="text" class="form-control" placeholder="Searching shifte..." @oninput="Filter" />
                </div>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Shifte_Name</th>
                                <th>StartTime</th>
                                <th>EndTime</th>
                                <th></th>
                            </tr>
                        </thead>

                        <tbody>
                            @foreach (var item in filter)
                            {
                                if (item != null)
                                {
                                    <tr>
                                        <td>@item.Shifte_Name</td>
                                        <td>@item.StartTime</td>
                                        <td>@item.EndTime</td>
                                        <td class="text-center">
                                            @if (!item.IsDeleted)
                                            {
                                                <button class="btn btn-primary" @onclick="() => EditShifte(item.Shifte_Id)">Sửa</button>
                                                <button class="btn btn-danger" @onclick="() => HideShifte(item.Shifte_Id)">Ẩn</button>
                                            }
                                            else
                                            {
                                                <button class="btn btn-warning" @onclick="() => RestoreShifte(item.Shifte_Id)">Khôi phục</button>
                                            }
                                        </td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
}
else if (!string.IsNullOrEmpty(errorMessage))
{
    <p class="text-danger">@errorMessage</p>
}
else
{
    <p>Loading...</p>
}

